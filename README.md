# mlx-unet
A demo U-Net made in MLX and trained on the MNIST dataset.

This is a translation of Nvidia's simple UNET model written in PyTorch created for educational purposes.

## Get Started
Use the `requirements.txt` file to get started!
`pip install requirements.txt`
